# Server configuration
PORT=5000
NODE_ENV=development
EMAIL_USER=kavindiarunika26@gmail.com
EMAIL_PASS=lbdyrkkpbyjvvifs   # the 16-character Gmail App Password you generated


# MongoDB connection
MONGO_URL='mongodb+srv://forever:forever123@cluster0.qbgajlz.mongodb.net/new'

# JWT Secret
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRY=30d

# CORS configuration
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Email configuration (optional)
# If not configured, emails will be silently skipped without errors
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here  # For Gmail, use App Password

# Admin notification settings
# Email address for receiving admin notifications (can be different from SMTP_USER)
ADMIN_EMAIL=admin@yourdomain.com
# Optional webhook URL for backup notifications
NOTIFICATION_WEBHOOK=https://discord.com/api/webhooks/your-webhook-url

# API Host (for generating URLs)
API_HOST=localhost:5000
